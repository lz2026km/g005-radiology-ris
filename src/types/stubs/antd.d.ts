/**
 * antd stub - the package ships its own type definitions (es/index.d.ts), so
 * no declarations are required. This file exists to document that decision and
 * to give the per-library organization a consistent layout.
 */
export {};
